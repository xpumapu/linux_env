# Start numbering at 1
set -g base-index 1
set-window-option -g pane-base-index 1

# Allows for faster key repetition
set -s escape-time 0

#set vi copy-mode
set-window-option -g mode-keys vi

#time status mesg are displayed
set-option -g display-time 5000

# Set status bar
set -g status-bg blue
set -g status-fg white
set -g status-left-bg green

set -g status-left "#S"
set -g status-right "U:#[fg=red]#H K:#(uname -r)"

#Active pane
setw -g display-panes-active-colour red
set -g display-panes-colour blue
#set -g mouse-select-pane on

set -g set-titles on
set -g set-titles-string "#T"

setw -g mode-bg white
set-window-option -g window-status-current-bg red

#terminal colors
set -g default-terminal "screen-256color"

# Set window notifications
#setw -g monitor-activity on
#set -g visual-activity on
#set -g default-command bash -i


#Define sessions

#Ath10k work
new-session -n homex0 -s dell-lap

new-window -n kernelx "loop ssh -t dell-lap \"cd ath; bash \" "

new-window -n kernel-compilationx "loop ssh -t dell-lap \"cd ath; bash \" "
split-window -v "loop ssh -t dell-lap \"cd ath; bash \" "

new-window -n kernel-gitx "loop ssh -t dell-lap \"cd ath; bash \" "
split-window -h "loop ssh -t dell-lap \"cd ath; bash \" "

new-window -n hostapx "loop ssh -t dell-lap \"cd hostap/hostapd; bash \" "

new-window -n hostap-splitx "loop ssh -t dell-lap \"cd hostap/hostapd; bash \" "
split-window -h "loop ssh -t dell-lap \"cd hostap/hostapd; bash \" "

new-window -n hostap-testx 
split-window -h "loop ssh -t dell-lap \"less /var/log/syslog; bash \" "
split-window -v "loop ssh -t dell-lap"

new-window -n homex1 "loop ssh -t dell-lap"

#Dell computer marek@dell
new-session -d -n homem0 -s dell-comp

new-window -n kernelm "cd ~/cdisk"

new-window -t dell-comp -n kernel-splitm "cd ~/cdisk" 
split-window -v "cd ~/cdisk"

new-window -t dell-comp -n kernel-gitm "ssh -t localhost \" cd ~/cdisk/atheros/wireless-testing; bash\""
split-window -h "ssh -t localhost \" cd ~/cdisk/atheros/wireless-testing; bash\""

new-window -t dell-comp -n hostapm "ssh -t localhost \" cd ~/cdisk/hostap; bash\""

new-window -t dell-comp -n hostap-splitm "ssh -t localhost \" cd ~/cdisk/hostap; bash\""
split-window -v "ssh -t localhost \" cd ~/cdisk/hostap; bash\""

new-window -t dell-comp -n hostap-gitm "ssh -t localhost \" cd ~/cdisk/hostap; bash\""
split-window -h "ssh -t localhost \" cd ~/cdisk/hostap; bash\""

new-window -t dell-comp -n vimm "vim ."

new-window -t dell-comp -n homem1


